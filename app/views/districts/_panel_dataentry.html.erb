<div id="data_panel_container" class="data-panel-container">
<div id="data_panel" class="data-panel">	
	<%= form_for(@district) do |f| %>
	  	<% if @district.errors.any? %>
			<div id="error_explanation">
		    	<h2><%= pluralize(@district.errors.count, "error") %> prohibited this log from being saved:</h2>

		      	<ul>
		      		<% @district.errors.full_messages.each do |msg| %>
		        	<li><%= msg %></li>
		      		<% end %>
		      	</ul>
		    </div>
		<% end %>

		<table class="data-entry">
			<tr>
				<th><%= f.label :name, "District" %></th>
				<td><%= f.text_field :name %></td>
			</tr>
			<tr>
				<th><%= f.label :province %></th>
				<td><%= collection_select(:district, :province_id, Province.default, :id, :name, :prompt => 'Please select...' ) %></td>
			</tr>
			<tr>
				<th></th>
				<td><%= f.submit %></td>
			</tr>
		</table>
	<% end %>
</div>
</div>